{
  "name": "discovery-swarm",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:build": "browserify -r fs:graceful-fs -e ./test/test-web.js > ./test/test-bundle.js",
    "test:webext": "env MOZ_DISABLE_CONTENT_SANDBOX=1 web-ext run -s ./test --firefox=nightly --no-reload",
    "postinstall": "mkdir -p test/vendor && cp node_modules/mocha/mocha.css test/vendor/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pump": "^3.0.0",
    "websocket-stream": "^5.1.2"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "chai": "^4.1.2",
    "hyperdrive": "^9.14.0",
    "libdweb": "git+https://github.com/mozilla/libdweb.git",
    "mocha": "^5.2.0",
    "random-access-memory": "^3.0.0",
    "web-ext": "^2.8.0"
  }
}
